//twMerge(buttonStyles({ buttonType: extractedProps.buttonType }), extractedProps.class)

hover:
active:
focus:

dark:hover:
dark:active:
dark:focus:

await new Promise((res) => setTimeout(res, 2000));
